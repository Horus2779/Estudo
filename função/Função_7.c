#include <stdio.h>
int lerNotas();
int calculaMedia();


int lerNotas(){
	int vet[10],pos;
	float nota;
	for(pos==0;pos<10;pos++){
		printf("Digite a %d nota:",pos+1);
		scanf("%f",&vet[nota]);
		if{
		}
	}
}
